block content
  h1= Index 
  p Welcome to Index page
  // Button with onclick attributes to call boost() function
  button(type="button", class="btn btn-primary", onclick="boost()") press me

  div#boost Add in extra 

  script.
    let value = 0;
    let amt = 3;
    function boost() {
      amt += 1;
      value += amt;
      document.getElementById("boost").innerText = `Boosted ${value} by ${amt}`;
    }

  // Add a link to the Sculptures page endpoint
  a(href='/sculptures') View Sculptures
